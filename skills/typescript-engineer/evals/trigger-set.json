[
  {
    "query": "getting `Type 'string | undefined' is not assignable to type 'string'` on line 42 of src/orders/tax.ts — help",
    "should_trigger": true
  },
  {
    "query": "src/lib/http.ts has `export function request(config: any) { return fetch(config.url, config.init) }` and every caller passes a different shape. i hate the `any` — make it generic so the return type follows what the caller sends",
    "should_trigger": true
  },
  {
    "query": "explain how `infer` works inside conditional types, specifically when distributing over a union",
    "should_trigger": true
  },
  {
    "query": "design a branded type for UserId so I can't accidentally pass an OrderId where a UserId is expected",
    "should_trigger": true
  },
  {
    "query": "in config/theme.ts i wrote `const theme = { primary: '#fff' } satisfies Theme` but theme.primary is still just `string` and the autocomplete on keys is gone?? thought satisfies was supposed to keep the literals",
    "should_trigger": true
  },
  {
    "query": "our router in src/routes.ts is typed as `Record<string, Handler>`. want a type that only accepts paths like `/users/:id` and gives the handler a typed params object with `id` in it — template literal thing?",
    "should_trigger": true
  },
  {
    "query": "createStore<S>(initial: S, overrides?: Partial<S>) keeps widening S to whatever the overrides contain, so passing `{ extra: 1 }` sneaks into the state type. heard NoInfer fixes this, how would you apply it here?",
    "should_trigger": true
  },
  {
    "query": "parseDate(input: string | number | Date) returns `Date | null` for all of them but really only the string case can be null. should this be three overloads or is a union return fine? users keep complaining about the null check",
    "should_trigger": true
  },
  {
    "query": "given `type Settings = { volume: number; muted: boolean; theme: string }` I want `Getters<Settings>` to produce `{ getVolume(): number; getMuted(): boolean; ... }` — how do I write the mapped type with the renamed keys?",
    "should_trigger": true
  },
  {
    "query": "we're moving src/legacy/queue.js to queue.ts, keep the runtime behaviour exactly the same but give it real types instead of a big `any` blob",
    "should_trigger": true
  },
  {
    "query": "add a zod schema to validate this incoming API payload and throw on mismatch",
    "should_trigger": false
  },
  {
    "query": "change this function to be async instead of sync",
    "should_trigger": false
  },
  {
    "query": "tsc says ENOENT: can't find ./src/index.ts — fix the paths in tsconfig",
    "should_trigger": false
  },
  { "query": "write a jest test for getUserById", "should_trigger": false },
  {
    "query": "refactor this React component to use hooks instead of classes",
    "should_trigger": false
  },
  {
    "query": "the build is failing with `Cannot find module '@/utils/date'` — likely a path alias issue",
    "should_trigger": false
  },
  {
    "query": "what's the best way to structure a monorepo with workspaces?",
    "should_trigger": false
  },
  {
    "query": "upgrade the typescript version in package.json to 5.5",
    "should_trigger": false
  },
  {
    "query": "add JSDoc types to lib/format.js — we don't use TS in this repo, just want editor hints",
    "should_trigger": false
  },
  {
    "query": "generate TS types from openapi.yaml with openapi-typescript and wire the script into package.json",
    "should_trigger": false
  }
]
