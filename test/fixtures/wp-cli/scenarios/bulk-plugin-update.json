{
  "name": "Update all plugins safely with backup",
  "prompt": "Update all plugins on my WordPress site. Make sure it's done safely.",
  "context": {
    "isDestructive": false
  },
  "expected": {
    "commands": ["wp plugin update", "wp db export"],
    "expectBackup": true,
    "expectDryRun": true,
    "expectWarnings": false
  }
}
